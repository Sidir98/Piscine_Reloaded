#include <unistd.h>
#include <stdio.h>

int ft_sqrt(int nb);

int main()
{
	int	a;
	//int t;

	a = 0;
	/*t = 2;
	while(t*t > 0)
	{
		printf("%d\n",t*t);
		t++;
	}*/
	printf("%d", ft_sqrt(a));
    return 0;
}
